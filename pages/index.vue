<script setup lang="ts">

</script>

<template>
    <div>
        <ContentList path="/posts" :query="{
            draft: false, sort: [{
                date: -1
            }]
        }" v-slot="{ list }">
            <div v-for="(post) in list" :key="post._path" class=" bg-white rounded-2xl overflow-hidden mb-4">
                <div class="h-[320px] relative">
                    <img :src="post.thumbnail" class="w-full h-full object-cover absolute">
                </div>
                <div class="blog-card--meta">
                    <h3 class="text-2xl font-semibold">
                        <NuxtLink :to="`/${post.slug}`">{{ post.title }}</NuxtLink>
                    </h3>
                    <div class="text-sm text-gray-500 mt-px">
                        {{ post.date }}
                    </div>
                </div>
            </div>
        </ContentList>
    </div>
</template>  